export function getProperty(obj, property) {
    return crsbinding.data.getProperty(obj, property);
}

export function setProperty(obj, property, value) {
    crsbinding.data.setProperty(obj, property, value);
}